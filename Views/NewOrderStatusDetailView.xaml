<UserControl x:Class="PDAB.Views.NewOrderStatusView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Status Name -->
        <Label Grid.Row="0" Grid.Column="0" Content="Status Name:" Margin="0,0,10,10"/>
        <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding StatusName}" Margin="0,0,0,10"/>

        <!-- Description -->
        <Label Grid.Row="1" Grid.Column="0" Content="Description:" Margin="0,0,10,10"/>
        <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding Description}" 
                 TextWrapping="Wrap" AcceptsReturn="True" Height="100" Margin="0,0,0,10"/>

        <!-- Save Button -->
        <Button Grid.Row="2" Grid.Column="1" Content="Save" 
                Command="{Binding SaveCommand}"
                HorizontalAlignment="Right" Width="80" Height="25"/>
    </Grid>
</UserControl>