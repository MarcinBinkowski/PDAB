<UserControl x:Class="PDAB.Views.AllOrderPaymentsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <ListView ItemsSource="{Binding List}">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="ID" DisplayMemberBinding="{Binding OrderPaymentId}" Width="50"/>
                    <GridViewColumn Header="Order ID" DisplayMemberBinding="{Binding OrderId}" Width="70"/>
                    <GridViewColumn Header="Payment Method" DisplayMemberBinding="{Binding PaymentMethod.MethodName}" Width="120"/>
                    <GridViewColumn Header="Amount" DisplayMemberBinding="{Binding Amount, StringFormat=C}" Width="100"/>
                    <GridViewColumn Header="Payment Date" DisplayMemberBinding="{Binding PaymentDate, StringFormat={}{0:g}}" Width="130"/>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</UserControl>